{
  "name": "local-api-web-proxy",
  "version": "1.0.0",
  "description": "Local HTTP proxy with Charles-inspired real-time dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "start:custom": "node server.js --proxy-port ${PROXY_PORT:-8888} --dashboard-port ${DASH_PORT:-8000}"
  },
  "dependencies": {
    "node-forge": "^1.4.0",
    "ws": "^8.18.0"
  }
}
